#include <bits/stdc++.h>
#include <tokens.h>
using namespace std;

//read file
void read_file()
{
    string word;
    ifstream file;
    file.open("text.txt");
    while(file >> word)
    {
        if(f)
    }
    file.close();
}

int main()
{
    read_file();
    return 0;
}
